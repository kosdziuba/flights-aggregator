import { Test, TestingModule } from '@nestjs/testing';
import { {{pascalCase name}}Controller } from './controller';

describe('{{pascalCase name}}Controller', () => {
  let {{camelCase name}}Controller: {{pascalCase name}}Controller;

  beforeEach(async () => {
    const app: TestingModule = await Test.createTestingModule({
      controllers: [{{pascalCase name}}Controller],
    }).compile();

    {{camelCase name}}Controller = app.get<{{pascalCase name}}Controller>({{pascalCase name}}Controller);
  });

  describe('root', () => {
    it('should return "Example"', async () => {
      expect(await {{camelCase name}}Controller.findAll()).toEqual({ data: 'Example' });
    });
  });
});
